# KarlZeo Dash

## How do I install these formulae?

`brew install karlzeo/dash/<formula>`

Or `brew tap karlzeo/dash` and then `brew install <formula>`.

## Documentation

`brew help`, `man brew` or check [Homebrew's documentation](https://docs.brew.sh).
